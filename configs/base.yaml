name: vqgan
seed: 12
model_checkpoint: null

base_lr: 4.5e-6
batch_size: 12
epochs: 20
steps_per_epoch: 0

model: ???
quantizer: ???
discriminator: null

optimizer:
  factory: torch.optim.Adam
  eps: 1e-8
  betas: [0.5, 0.9]
  weight_decay: 0.0

rampup: null
cosine_annealing: False
clip_grad_norm: null

adv_start_step: 0

loss_weights:
  pixel: 1.0
  perceptual: 1.0
  adv: 0.8

pixel_loss: torch.nn.L1Loss
perceptual_loss: null
adv_loss: softfsq.losses.GeneratorWGANLoss
disc_loss: softfsq.losses.DiscriminatorHingeLoss

compile: False
profile: False
wandb: False
checkpointing: True
